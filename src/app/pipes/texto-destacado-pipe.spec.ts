import { TextoDestacadoPipe } from './texto-destacado-pipe';

describe('TextoDestacadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoDestacadoPipe();
    expect(pipe).toBeTruthy();
  });
});
